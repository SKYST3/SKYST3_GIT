import { IoGlobeOutline } from "react-icons/io5";

import { ReactIconProps } from "@/types";

export default function GlobeIcon({ size, color }: ReactIconProps) {
  return <IoGlobeOutline color={color} size={size} />;
}
