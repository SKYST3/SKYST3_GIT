import { IoIosRefresh } from "react-icons/io";

import { ReactIconProps } from "@/types";

export default function RefreshIcon({ size, color }: ReactIconProps) {
  return <IoIosRefresh color={color} size={size} />;
}
