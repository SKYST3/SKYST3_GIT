import { PiDotsThreeCircleLight } from "react-icons/pi";

import { ReactIconProps } from "@/types";

export default function ThreedotsIcon({ size, color }: ReactIconProps) {
  return <PiDotsThreeCircleLight color={color} size={size} />;
}
