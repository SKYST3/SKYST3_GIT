import { LuGripVertical } from "react-icons/lu";

export default function GripVerticalIcon({
  size,
  color,
}: {
  size: number;
  color: string;
}) {
  return <LuGripVertical color={color} size={size} />;
}
